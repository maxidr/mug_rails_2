<div class="nested-fields">
  <div class="field">
    <%= f.label :product_id %><br />
    <%= f.select :product_id, options_from_collection_for_select(Product.all, :id, :name, f.object.product_id.to_i) %>
  </div>
  <div class="fields">
    <%= f.label :quantity %><br />
    <%= f.text_field :quantity %>
  </div>

  <%= link_to_remove_association "remove item", f %>

</div>

